# Whisper ASR Tests (faster-whisper and HuggingFace Whisper)
# Usage: uv pip install -r requirements.txt

# Environment configuration
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0
tqdm>=4.65.0

# Malay NLP processing
malaya>=5.1.0
pypi-kenlm
PySastrawi

# Evaluation metrics
torchmetrics>=1.0.0

# PyTorch and audio
torch>=2.0.0
torchaudio>=2.0.0

# Whisper models
faster-whisper>=1.0.0
transformers>=4.35.0
huggingface-hub>=0.20.0
